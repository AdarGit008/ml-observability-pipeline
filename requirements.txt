# Runtime dependencies for the ML Observability Pipeline.
#
# Kept minimal on purpose. Each new line should be justified in the session
# log that added it; see DEV_NORMS section 6.2.
#
# Dev-only deps (pytest, ruff, mypy, etc.) will land in requirements-dev.txt
# once we add CI or a dev_workflow session declares them.

PyYAML>=6.0                # simulator/config.py + local_runtime/config.py — config-yaml session 2026-05-25
aiomqtt>=2.0               # simulator/publisher.py + local_runtime/subscriber.py — mqtt session 2026-05-25; asyncio-native wrapper around paho-mqtt by the same maintainer (see ADR 0003)
paho-mqtt>=2.0             # transitive dep of aiomqtt; pinned explicitly so cert/QoS knobs we use are visible in this file
numpy>=1.26                # shared/features.py — pure-numpy rolling mean/std for the 8-feature extractor (mode-parity boundary; see ADR 0005). local_runtime session 2026-05-29.
influxdb-client[async]>=1.40  # local_runtime/influx_writer.py — official Python client for InfluxDB v2.x with the [async] extra (aiohttp-backed InfluxDBClientAsync). The [async] extra is load-bearing per Gemini Q4 of the 2026-05-29 review. local_runtime session 2026-05-29.
scikit-learn>=1.4         # shared/score.py — HistGradientBoostingClassifier.predict_proba for the live scorer (real model lands in shared/score via shared.score._load_classifier). model session 2026-06-01.
joblib>=1.3               # transitive of scikit-learn but pinned explicitly because shared/score.py uses joblib.load directly to read the trained bundle written by model.train.write_artifacts. model session 2026-06-01.
boto3>=1.34               # lambda_scorer/handler.py (DynamoDB per ADR 0010) + dashboards_adapter/handler.py (BatchGetItem per ADR 0014). NOT bundled in the deploy zips — the Lambda runtime provides it; bundling botocore would blow the 50 MB direct-upload limit (infra session 2026-06-04). Local dev/test dep only.
pyarrow>=14.0               # lambda_s3_batcher/handler.py — Parquet writer (ADR 0015; no pandas). Bundled for Lambda via scripts/batcher_requirements.txt (pinned ==24.0.0 there); this line covers local tests. cold-path session 2026-06-04.
