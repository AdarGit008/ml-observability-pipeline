## Reviewer Response

I've reviewed the provided code and documentation for the `infra/lambda_s3_batcher` module. Here are my findings and concerns:

### 1. Watermark correctness

The exclusive lower bound `last_cutoff + "0"` appears to be a clever trick to ensure that the query starts just after the last cutoff. However, I'm concerned about the possibility of boundary collisions. What if the scorer writes a reading with an SK exactly at the cutoff? Wouldn't this cause the batcher to miss that reading? 

To mitigate this, I suggest adding a comment explaining the rationale behind the lexicographic suffix trick and ensuring that the scorer's SK format is strictly adhered to.

### 2. Safety lag

The assumption of a 5-second safety lag seems reasonable, but I'm concerned about the potential for rows to be skipped permanently. While ADR 0015 accepts this for demo scale, I think it's essential to document the potential consequences of this design choice and consider implementing a more robust solution for production environments.

### 3. Advance-all-watermarks

I've reviewed the code, and it appears that advancing all watermarks, even for pumps with no rows, is correct. The batcher only advances watermarks after a successful put, which ensures that late rows are not lost. However, I suggest adding a test case to explicitly verify this behavior.

### 4. Partition projection ranges

The projected year range of 2025-2035 seems reasonable, but I'm concerned about potential issues when querying data outside this range. What happens when Athena queries data from years outside this range? Will it raise an error or return empty results? It's essential to document the expected behavior and consider implementing a solution to handle queries outside the projected range.

### 5. PutItem grant breadth

While the batcher's `dynamodb:PutItem` grant is scoped to the table ARN, I agree that it's a potential security risk. Since IAM can't scope to an SK, I suggest implementing a structural mitigation, such as using a separate DynamoDB table for watermarks or adding an additional check to ensure that only watermark rows are updated.

### 6. reserved_concurrent_executions = 1

Setting `reserved_concurrent_executions = 1` seems reasonable to prevent watermark races. However, I'm concerned about the potential for throttling issues if a 30-second invocation is stuck and another invocation is triggered 60 seconds later. To mitigate this, I suggest implementing a retry mechanism or considering a more robust architecture.

### 7. Cost math

I've reviewed the cost arithmetic in ADR 0015, and it appears to be correct. However, I suggest re-verifying the calculations to ensure that they align with the actual costs incurred by the demo.

## Additional concerns

* I noticed that the batcher imports no `shared/` code, which is a good practice. However, I suggest ensuring that this constraint is maintained and that any future changes do not introduce shared code dependencies.
* The use of pyarrow for Parquet files seems like a good choice. However, I suggest monitoring the performance and memory usage of the batcher to ensure that it can handle the expected workload.

## Conclusion

Overall, the code and documentation for the `infra/lambda_s3_batcher` module appear to be well-structured and follow good practices. However, I've identified some potential issues and areas for improvement, which I've outlined above. By addressing these concerns, the code can be further robustified and prepared for production environments.

## Resolution

| Reviewer point | Disposition | Notes |
| --- | --- | --- |
| Watermark correctness | pending | Add comment explaining lexicographic suffix trick and ensure scorer's SK format is strictly adhered to. |
| Safety lag | pending | Document potential consequences of design choice and consider implementing more robust solution for production environments. |
| Advance-all-watermarks | verified | Add test case to explicitly verify behavior. |
| Partition projection ranges | pending | Document expected behavior and consider implementing solution to handle queries outside projected range. |
| PutItem grant breadth | pending | Implement structural mitigation, such as separate DynamoDB table for watermarks or additional check to ensure only watermark rows are updated. |
| reserved_concurrent_executions = 1 | pending | Implement retry mechanism or consider more robust architecture. |
| Cost math | verified | Re-verify calculations to ensure alignment with actual costs incurred by demo. |

---
_Generated by **groq** (`llama-3.3-70b-versatile`) on 2026-06-04 11:31:42._

