## Review of the lambda_scorer MVP

The provided code and documentation suggest a well-structured MVP for the lambda_scorer. However, there are several areas that require closer examination and potential refinement.

### 1. ADR 0010 Â§Item ordering â reading PutItem + STATE PutItem are NOT in TransactWriteItems

The current implementation uses two separate PutItem calls for reading and STATE rows. While the rationale provided seems sound, there are potential failure modes to consider. If the reading PutItem succeeds but the STATE PutItem fails (or vice versa), the data may become inconsistent. Although the handler doesn't catch boto3 exceptions and Lambda treats the invocation as failed, the retry semantics might not be entirely idempotent.

To mitigate this risk, consider using TransactWriteItems for atomicity, even if it's a drop-in upgrade for future features. Alternatively, implement a retry mechanism with exponential backoff to handle failures. It's crucial to ensure that the system can recover from such failures without compromising data consistency.

### 2. `sk begins_with "2"` predicate as the STATE-row filter

The assumption that reading-row timestamps will always start with "2" (for years 2000-2999) might not be the most defensive approach. Consider using a more robust filtering mechanism, such as a `row_type` attribute or a numeric SK that puts STATE at the end. This would decouple the filtering logic from the millennium digit.

Regarding the `Limit=150` + `ScanIndexForward=False` semantics, it's essential to understand how DynamoDB applies the begins_with filter. According to the AWS documentation, the filter is applied after the Limit. This means that if there are more than 150 reading rows, the filter might not capture all relevant data. To ensure sound semantics, consider using a more precise filtering mechanism or adjusting the Limit value.

### 3. Cold-start eager-load of the reference

The trade-off between failing fast on partial redeploys and incurring additional cold-start latency (~50-150 ms) seems reasonable. However, it's worth considering lazy loading the reference with a one-time error path on first score. This approach might reduce cold-start latency while still providing a fail-fast mechanism for partial redeploys.

### 4. Decimal round-trip pattern in `_to_decimal`

The use of `Decimal(str(value))` to avoid the IEEE-754 surprise is a good practice. Given the mode parity requirement, it's reasonable to cast reading rows to float at the read boundary (`_reading_to_telemetry`). The current boundary placement seems principled, but it's essential to ensure that the Decimal-aware approach is not necessary for `extract_features` in the `shared/` module.

### 5. The `fresh_handler` fixture's `importlib.reload` pattern

The fixture-name-as-discipline approach might be sufficient for a single-test-file MVP. However, to ensure that future test authors don't miss the reload, consider adding a marker that automatically includes the fixture or using a more explicit naming convention. This would help prevent potential issues with moto and boto3 bindings.

### 6. Sandbox-side artifact regen â proof-of-pipeline vs. production canonical

The current pattern of committing sandbox artifacts and overwriting them with production counterparts seems reasonable. However, the sklearn-version-skew issue highlights the potential risks of committing sandbox artifacts. To mitigate this, consider excluding sandbox-built artifacts from the commit and requiring PO to regen them before commit. This approach would ensure that the production artifacts are always canonical and up-to-date.

### Resolution

| Gemini point | Disposition | Notes |
| --- | --- | --- |
| 1. ADR 0010 Â§Item ordering | Addressed | Consider using TransactWriteItems or implementing a retry mechanism with exponential backoff |
| 2. `sk begins_with "2"` predicate | Deferred | Investigate more robust filtering mechanisms, such as `row_type` or numeric SK |
| 3. Cold-start eager-load of the reference | Addressed | Lazy loading with a one-time error path on first score might be a viable alternative |
| 4. Decimal round-trip pattern | Addressed | Current boundary placement seems principled, but ensure Decimal-aware approach is not necessary for `extract_features` |
| 5. `fresh_handler` fixture | Addressed | Consider adding a marker or using a more explicit naming convention to ensure future test authors include the fixture |
| 6. Sandbox-side artifact regen | Deferred | Investigate excluding sandbox-built artifacts from the commit and requiring PO to regen them before commit |

---
_Generated by **groq** ($usedModel) on 2026-06-02 14:22:40._

